{
  "name": "jund",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun --filter '*' build",
    "check": "bun --filter '*' check",
    "test": "bun --filter '*' test",
    "format": "oxfmt --write .",
    "lint": "oxlint --fix"
  },
  "devDependencies": {
    "oxfmt": "^0.43.0",
    "oxlint": "^1.58.0",
    "typescript": "^6.0.2"
  }
}
